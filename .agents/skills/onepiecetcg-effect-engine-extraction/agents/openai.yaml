interface:
  display_name: "OPTCG Effect Engine Extraction"
  short_description: "Extract the effect engine from the API app"
  default_prompt: "Use $onepiecetcg-effect-engine-extraction to move the effect engine into its own reusable package."
